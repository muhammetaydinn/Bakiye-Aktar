name: kirkuc_numara
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.12.0
  firebase_auth: ^3.3.7
  cloud_firestore: ^3.1.8
  firebase_storage: ^10.2.7
  mobx: ^2.0.6+1
  flutter_mobx: ^2.0.4
  flutter_screenutil: ^5.2.0
  cupertino_icons: ^1.0.2
  auto_size_text: ^3.0.0
  google_fonts: ^2.3.1
  flutter_svg: ^1.0.3
  google_maps_flutter: ^2.1.1
  location: ^4.3.0
  flutter_credit_card: ^3.0.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^2.0.5+2  
  build_runner: ^2.1.7
  flutter_lints: ^1.0.0
  

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/svg/
    - assets/svg/bank_withdraw_blue.svg


